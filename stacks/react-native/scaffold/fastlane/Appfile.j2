# Fastlane Appfile — fill these in for your app.
# iOS (App Store Connect)
app_identifier("com.example.app")        # iOS bundle identifier
apple_id("you@example.com")              # Apple ID email (optional with API key auth)
itc_team_id("REPLACE_ME")                # App Store Connect team id (optional)
team_id("REPLACE_ME")                    # Apple Developer team id (optional)

# Android (Google Play) — supply reads json_key from the lane.
package_name("com.example.app")          # Android applicationId
