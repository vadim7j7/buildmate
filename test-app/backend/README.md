# Todo API Backend

A FastAPI backend for a Todo application.

## Setup

```bash
uv sync --all-extras
```

## Running

```bash
uv run uvicorn app.main:app --reload
```

## Testing

```bash
uv run pytest
```
